const PORT = 3000;
const HOSTNAME = 'localhost';
const SERVER_FILES_DIR = './files_server';
const CLIENT_FILES_DIR = './files_client';

module.exports = {
  PORT, HOSTNAME, SERVER_FILES_DIR, CLIENT_FILES_DIR,
};
